# SignalWire agent basic-auth credentials (optional; SDK auto-generates if unset)
SWML_BASIC_AUTH_USER=
SWML_BASIC_AUTH_PASSWORD=

# Appointment API endpoint (defaults to the shared mock server)
APPOINTMENT_API_URL=https://apptsrv-b98a1588311b.herokuapp.com

# Port the agent listens on
PORT=3000
